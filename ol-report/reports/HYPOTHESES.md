# OL Report Hypotheses - Permanent Magnet Synchronous Motor (PMSM) Physics under Label Scarcity

**Frozen: 2026-08-15. This file is the pre-registration of record for this study.**

Freeze rule: no silent edits, ever. If a hypothesis turns out ill-posed, the correction is a dated entry under Amendments, disclosed in the report. Evidence available at freeze time: the study plans, the dataset first-look scan (row and session counts, units, value ranges), and the literature index. No EDA notebook had run and no line of model code existed.

## Hypotheses

- **H1 (headline):** the PINN degrades most gracefully. At 3 % labels and below it beats every other rung on both `pm` and `torque`, with the margin over the data-driven models growing monotonically as labels shrink. B0 stays flat but never wins, because it cannot learn what the LPTN leaves out.
- **H2:** adaptive $\lambda$ weighting beats fixed scale-normalized weights on validation loss and cross-seed stability.
- **H3:** term ablation is target-specific. Removing `L_thermal` hurts `pm` under sparsity more than removing the voltage residuals hurts torque.
- **H4:** collocation earns its keep only under sparsity. At full labels 0× vs 4× differs by under 5 % MAE, at 3 % labels 4× beats 0× by over 15 %.
- **H5:** baseline degradation is not seed noise. At 1 % labels every data-driven rung's cross-seed MAE spread is smaller than its gap to the PINN.
- **H6:** intervals stay honest, and physics keeps them tight. Split-conformal coverage holds within 5 points of the 90 % target for every rung at every level, and the PINN's interval width at 1 % labels is under 1.5× its full-label width while every data-driven rung's exceeds 3×.

## Resolution protocol

Each hypothesis is resolved accept, reject, or qualify in the study's closing notebook, with the numeric criteria above taken literally. A claim stands only if the three-seed spread supports it. The foundation script verifies this file is unchanged since the freeze (amendments excepted), and the invariant suite checks it exists and contains every hypothesis.

## Amendments

- **2026-08-15 - Notation converted to LaTeX math.** Typographic conversion only, no claim, threshold, or wording of any hypothesis changed.
- **2026-08-15 - H1 restated for the eighth rung (second reviewer, inductive bias).** sl added the P2 structured-coupled hybrid, so H1's claim now reads: coupled physics degrades most gracefully. At 3 % labels and below the coupled rungs (P and P2) beat every non-coupled rung on both `pm` and `torque`, with margins growing monotonically as labels shrink. Which injection mechanism wins between P and P2 is left open and reported. The grid grows to 8 rungs × 5 levels × 3 seeds = 120 conditions. The original PINN-centric phrasing stands recorded above, unedited.
- **2026-08-15 - H1 restated as best-vs-best, seeds widened at the bottom rungs (third reviewer).** The conjunction was fragile: P losing to B5 at 1 % labels would fail H1 even where the coupling thesis holds. H1 now reads: the best coupled rung (P or P2) beats the best non-coupled rung (B0 through B5) on both `pm` and `torque` at 3 % labels and below, with the margin growing monotonically as labels shrink. The P-versus-P2 mechanism comparison is the reported secondary. The seed set widens to five (7466-7470) at the 3 % and 1 % levels, growing the grid to 152 conditions. The prior phrasings stand recorded above, unedited.
- **2026-08-15 - H5 and H6 aligned with best-vs-best, calibration isolated (fifth review).** H5 now reads: at 1 % labels every non-coupled learned rung's (B1-B5) cross-seed MAE spread is smaller than its gap to the best coupled rung. H6 now reads: split-conformal coverage holds within 5 points of the 90 % target for every rung at every level, and the best coupled rung's interval width at 1 % labels is under 1.5× its full-label width while every non-coupled learned rung's exceeds 3×. Conformal calibration moves to the reserved `VAL_CAL` sessions (3 of the 6 validation sessions, untouched by early stopping and the pilot). Ensemble membership follows the seed plan (3 members at the top three levels, 5 at the bottom two), and cross-level width comparisons use matched 3-member subsets with 5-member widths as robustness. The original phrasings stand recorded above, unedited.
- **2026-08-15 - H6 moved to session-level conformal with nine calibration sessions (sixth review).** Three calibration sessions cannot yield finite 90 % split-conformal intervals ($n \geq 1/\alpha - 1 = 9$), and row-level residuals within a session are not exchangeable, so the effective sample was three, not thousands. The split becomes 22 train / 3 `VAL_STOP` / 9 `VAL_CAL` / 6 test. The conformity score is per-session (the within-session 90th percentile of absolute residuals), the threshold is the conformal quantile across the nine session scores, and H6's coverage claim takes the 90/90 session-level form, checked empirically on test within 5 points, per rung and level. Width claims are unchanged. The prior phrasings stand recorded above, unedited.
- **2026-08-16 - Test grows to nine sessions, coverage acceptance becomes one-sided (seventh review).** With six test sessions, session-level coverage moves in 16.7 % steps and the within-5-points check was unsatisfiable at every achievable value. The split becomes 19 train / 3 `VAL_STOP` / 9 `VAL_CAL` / 9 test. H6's coverage acceptance is now: at least 8 of the 9 test sessions covered, per rung and level, one-sided because the conformal guarantee is a lower bound and over-coverage is not a failure (9/9 occurs with probability near 0.39 under perfect calibration). Width claims are unchanged. If the adequacy-gate decision moves to the full dataset, the split re-scales with `VAL_CAL` and test each keeping at least nine sessions. The prior phrasings stand recorded above, unedited.
- **2026-08-16 - Coverage becomes descriptive, width becomes the confirmatory claim (eighth review).** Requiring at least 8 of 9 covered sessions in all 40 rung-level conditions fails by chance: under true 90 % coverage one condition passes with probability 0.775, so across forty conditions some failure is near-certain even under perfect calibration. H6 now reads: the 90/90 conformal guarantee is theoretical under session exchangeability. Empirical coverage is reported descriptively as covered test sessions out of nine per rung and level with exact binomial confidence intervals, and no per-condition threshold is enforced. The confirmatory claim is the width comparison: the best coupled rung's interval width at 1 % labels is under 1.5× its full-label width while every non-coupled learned rung's exceeds 3×. The prior phrasings stand recorded above, unedited.
