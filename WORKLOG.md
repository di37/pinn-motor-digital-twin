# Worklog - Project Root

Newest day first. Every task that changes this folder gets an entry the same day: what was done, why when it matters, and the files touched. If it happened and left no worklog line, it did not happen.

## 2026-08-16

- **Gate decision recorded and Part 0 closed on the full dataset.** User chose full-dataset after the gate failed G1 and G2. 00c froze the 32/5/16/16 grouped split (566,601 / 70,064 / 342,792 / 351,359 rows), five CV folds over 32 training sessions, the train-only scaler, and the hypotheses hash. Notebook 02b built and executed: re-derives the assignment from seed, asserts equality with the manifests, audits envelope and duration balance across splits. Stale 19/3/9/9 banner strings updated. All uncommitted.
- **System diagram redrawn in mermaid.** The README's ASCII sketch replaced with a mermaid input-system-output flow (inputs, feature layer, the eight-rung system, training-time physics with the parameter head, and the three output kinds: predictions, conformal intervals, identified parameters). Renders natively on GitHub. Uncommitted.
- **Data dictionary rebuilt properly.** User flagged it: the first version had no description column and conflated observed with valid ranges. Now per column: physical meaning, unit, dtype, role, valid audit bounds distinct from observed ranges (zero violations), and per-column assumptions. Notebook 01 re-executed, CSV regenerated. Uncommitted.
- **Phase B executed to the gate, which FIRED.** 00a fingerprint verified (sha256 recorded), 00b drew the candidate sample (99,998 rows, 40 sessions), notebooks 01-02 built and executed (34 + 32 cells, 11 figures, data dictionary, session-quality table, risk register, baselines). Gate verdict on the real draw: G1 FAIL (envelope Jaccard 0.942 passed, but coolant quantile shift 0.122 vs 0.05), G2 FAIL (arc capture 0.253 vs 0.6), G3 PASS (62.5 % curved blocks). Per the pre-registered rule execution paused before 00c, which is written with a refuse-until-decision guard. Decision now with the user: keep-100k or full-dataset.
- **Phase A executed.** pinn-sl conda environment built and pins frozen (python 3.12, torch 2.8.0 with MPS verified, pandas 3.0, xgboost 3.4), sl-report scaffolded, `constants.py` written carrying every reviewed decision (seeds, 19/3/9/9 splits, gate thresholds, grids, budgets, amended tolerances), `run_logging.py` ported in the family pattern. Imports-clean gate passed. Committed as 88cbf89 onto the existing github.com/di37/pinn-motor-digital-twin repo.
- **H6 coverage made descriptive, width confirmatory (eighth review).** The 8-of-9 per-condition acceptance failed by chance across 40 conditions (0.775 per-condition pass rate under perfect calibration). Coverage is now reported with exact binomial confidence intervals under the theoretical 90/90 guarantee, and the width comparison carries the falsifiable physics claim. Fourth dated amendment on ol H6.
- **Nine test sessions, one-sided coverage acceptance (seventh review).** Six test sessions made the ±5-point coverage check unsatisfiable (16.7 % steps). Splits now 19/3/9/9, H6 accepts at ≥ 8 of 9 sessions covered per rung and level, over-coverage is not a failure, third dated amendment in ol. A full-dataset gate outcome keeps VAL_CAL and test at nine or more sessions.

## 2026-08-15

- **Nine-session conformal design (sixth review).** Splits revised to 22 train / 3 VAL_STOP / 9 VAL_CAL / 6 test, the minimum calibration count for finite 90 % intervals. Conformity scores are session-level (within-session 90th percentile of absolute residuals), the claim takes the 90/90 form, and ol's frozen H6 carries the dated amendment.
- **Split explicitly gated on sample-vs-full similarity.** User confirmation codified: 00c makes the train/val/test split only after notebook 02's comparison passes or the gate decision is recorded.
- **Fifth review's six fixes applied.** Validation partitioned into VAL_STOP and VAL_CAL so conformal calibration is isolated from selection, sl invariant 2 made manifest-driven so a full-dataset gate outcome cannot fail it, collocation opened to both coupled rungs, the injection grid relabeled honestly (P2 = hard thermal structure with soft electromagnetic coupling), ol H5/H6 aligned with best-vs-best by dated amendment, and ensemble membership matched to the seed plan with 3-member subsets for cross-level width comparisons.
- **550k fallback removed by user decision.** The sample-adequacy gate keeps its three criteria, but a failed gate now pauses execution and returns the decision to the user (keep 100k with disclosed limitations, or full dataset) instead of auto-escalating to the pre-committed two-hour-block design.
- **Grouped cross-validation added to sl selection.** Hyperparameter selection moves from the single 6-session validation split to 5-fold GroupKFold over the 28 training sessions, the coursework CV_FOLDS convention. The fixed validation split keeps early stopping, recipe export, and ol's calibration duties. New sl invariant 12 verifies folds never touch validation or test sessions.
- **Sample decision converted to a pre-registered gate (fourth review).** The 100k block sample is now a candidate judged in notebook 02 by three fixed criteria (envelope, thermal-arc capture at 0.6, curvature by ΔAIC), with the reviewer's 550k stride-8 design as the specified no-relitigation fallback. The reviewer's four exact numbers were verified against the raw file, one traversal figure could not be reproduced (43 min vs 131 under the natural metric). Independent of the gate: the mask-visible initialization rule landed in ol with new invariant 12, and the coupling diagnostic now logs α_mag alongside α_cu.
- **Working sample revised to ~100k rows (10 % of the file).** One seeded contiguous block per each of 40 stratified sessions, proportional lengths, 28/6/6 grouped splits preserved, state-bearing rungs initialize from measured block-start temperatures. The long-arc cost is disclosed in the sl plan. User decision, final condition before execution.
- **Third reviewer's five changes applied.** sl H5 clause restated (strongest rung without electromagnetic coupling), P2 brought under Part 1 synthetic validation and invariant 7, ol H1 restated as best-coupled vs best-non-coupled, the coupling diagnostic pre-registered as Phase D's first deliverable, the tripwire recalibrated (self-normalizing trigger, cheap pruned-subset response), matched labeled-batch schedule added to the fairness rule, and seeds widened to five at the bottom two sparsity levels (grid 152).
- **Dimensionality tripwire pre-registered in sl.** Two concrete triggers (PCA intrinsic-dimension check, train-validation gap check) that pull fe's feature selection forward before ol if either fires. The ul/fe deferral is confirmed as deferred, never cancelled.
- **Second reviewer's five changes applied.** ul and fe deferred to post-paper, targets trimmed to `pm` + `torque` + `stator_winding` (resolving the standing scope question), extra-credit parts marked optional, the P2 structured-coupled rung added (eight rungs, 120-condition ol grid, completing the injection-by-coverage two-by-two), and ol H1 restated so the headline claim tracks coupled physics rather than one injection mechanism. Frozen files amended with dated entries.
- **Contribution statement canonized.** The reviewer's one-sentence contribution added verbatim to the master plan's paper-scope section and the root README, as the sentence the project reuses wherever it describes itself.
- **Math converted to LaTeX everywhere.** Display equations as dollar-dollar blocks (dq voltages, torque, LPTN, temperature-affine parameters, composite loss), physical quantities inline as math, dataset columns kept as code. The convention is codified in the master plan voice rules. Frozen hypotheses files carry a dated notation-only amendment.
- **Repository named.** `pinn-motor-digital-twin`, recorded in the master plan. The local folder stays `pinn-project-eee`. Git init still lands in sl Phase A.
- **Planning sketch retired.** `physics-informed-pmsm-digital-twin.png` moved to the Trash after its diagram, loss, and ladder were redrawn in the README. One caveat recorded: the screenshot's bottom paragraph was cut off and never recovered.
- **Stale paper counts fixed.** The master plan and root README trees said 13 peer-reviewed references while the index records 17 with mixed review status. Both now match the index. Flagged by the reviewer.
- **Novelty-review changes applied (all five).** Competing-work sweep added to the literature index (3 stored, 2 listed), umbrella framing retitled from sparse sensors to scarce supervision, sl H3 amended to per-parameter tolerances, the PINN parameter head made temperature-affine with xai H4 restated on learned coefficients, and a paper-scope section added to the master plan (paper = sl + ol + xai Parts 2-3 + fe H5).
- **PINN paper of record added to the literature base.** The published JCP version (merging arXiv Parts I and II) supplied by the user, verified, renamed, and indexed. `research-paper/` now holds 14 papers.
- **Hypotheses frozen across all five studies.** Each study's `reports/HYPOTHESES.md` written and frozen (sl H1-H5, ol H1-H6, ul H1-H6, fe H1-H5, xai H1-H5). Foundation scripts flip from writing to verifying. Plans, READMEs, and the EDA plan updated to match.
- **Architecture diagram redrawn in the README.** The planning sketch (`physics-informed-pmsm-digital-twin.png`) recreated as an ASCII diagram in the root README with the composite loss, labels aligned to the dataset's real signals.
- **Worklogs created.** This file plus one per report folder and one in `research-paper/`. The always-update rule is now standing practice for every task.
- **`.gitignore` created.** Ignores the raw CSV, caches, transient logs, and reference PDFs. Processed data and checkpoints stay committed, family convention.
- **READMEs written.** Root plus all five studies, in the family README structure (abstract, Setup, Data, Run, Outputs) with honest plan-stage status lines.
- **Literature base built.** `research-paper/` with 13 peer-reviewed papers (arXiv author copies) and a venue-documented index. Raissi Part II (discovery) added on review.
- **EDA plan written.** `sl-report/EDA_PLAN.md` following the OMSCS 7641 nine-phase EDA guide, adapted to time-series sessions.
- **Titles fixed.** Permanent Magnet Synchronous Motor (PMSM) spelled out in every title. Typographic shorthand symbols banned after a § slipped into the EDA plan.
- **`src/` contents specified.** Module-by-module tables in every plan under the ol/ul contract (stdlib-only constants leaf, frozen configs, `(frame, summary)` runners).
- **Script frame aligned to ol/ul exactly.** Experiment scripts within 00-05, part names in filenames, condition-builder grouping, tail always 06/07/08. Folder-structure trees added to every plan from the GitHub screenshot.
- **Four upgrades folded in.** Seven-rung ladder (LPTN pure-physics floor, TNN state-of-the-art family), conformal uncertainty in ol, HMM temporal regimes in ul, three-way parameter cross-check in xai.
- **Project split into five studies.** sl / ol / ul / fe / xai, each with its own PLAN.md, mirroring the coursework repo layout. Master PLAN.md as the map. Data moved to `sl-report/data/`.
- **Dataset downloaded and verified.** Kaggle `wkirgsn/electric-motor-temperature` v3, 1,330,816 × 13, 69 sessions, 184.8 h at 2 Hz, physical units, torque 100 % present. Two plan risks retired.
- **Project started.** PMSM digital twin concept reviewed from the planning screenshot. Monolithic plan drafted, then iterated: sl-report conventions absorbed (EDA audit, per-model notebooks, primary-metric rule), writing voice codified (no em dashes, rare semicolons), sample EDA stage added, explainability made a study.
